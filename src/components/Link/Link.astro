---
import clsx from "clsx";
import * as styles from "./Link.css";

interface Props {
  href: string;
  className?: string;
}

const { href, className } = Astro.props;
---

<a href={href} target="_blank" class={clsx(styles.link, className)}>
  <slot />
</a>
